This directory stores legacy migrations that were removed from the active Supabase CLI chain during the 2026-04-20 baseline reset.

- `pre_baseline/` contains the old historical SQL files exactly as they existed before the reset.
- These files are kept for reference and historical review only.
- Do not move them back into `supabase/migrations/`.
